# Use /api to proxy through Next.js → Go backend (no CORS issues in dev).
# Or set http://localhost:8080 for direct calls (requires CORS on API).
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_API_KEY=

# Next.js rewrite target (server-side proxy)
API_PROXY_TARGET=http://localhost:8080
